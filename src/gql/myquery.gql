query MyQuery {
  repository(owner: "octocat", name: "Hello-World") {
    issues(last: 20, states: CLOSED) {
      edges {
        node {
          title
          labels(first: 5) {
            edges {
              node {
                name
              }
            }
          }
        }
      }
    }
  }
}
